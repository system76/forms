import { Meta, Preview, Props, Story } from '@storybook/addon-docs/blocks'

import SysFormEmail from './sys-form-email'

<Meta title="High Level Components|Email" component={SysFormEmail} />

# Email

This is an email entry field. It inherits from the high level `sys-form-input`
component.

## Props

<Props of={SysFormEmail} />

### Normal

This is how the input looks without any given props or data.

<Preview>
  <Story height="200px" name="Normal">
    {{
      data: () => ({
        email: ''
      }),
      template: `
        <sys-form-email v-model="email" id="email" />
      `
    }}
  </Story>
</Preview>
