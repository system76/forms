import { Meta, Preview, Props, Story } from '@storybook/addon-docs/blocks'

import SysFormPassword from './sys-form-password'

<Meta title="High Level Components|Password" component={SysFormPassword} />

# Password

This is a password entry field. It inherits from the high level `sys-form-input`
component, but includes masking (default html password input behavior), and
common validation rules for System76 passwords.

## Props

<Props of={SysFormPassword} />

### Normal

This is how the input looks without any given props or data.

<Preview>
  <Story height="200px" name="Normal">
    {{
      data: () => ({
        password: ''
      }),
      template: `
        <sys-form-password v-model="password" id="password" />
      `
    }}
  </Story>
</Preview>
