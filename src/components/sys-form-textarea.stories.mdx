import { Meta, Preview, Props, Story } from '@storybook/addon-docs/blocks'

import SysFormTextarea from './sys-form-textarea'

<Meta title="High Level Components|Textarea" component={SysFormTextarea} />

# Textarea

This is a high level textarea input that includes the label, input, validation,
and error message handling.

## Props

<Props of={SysFormTextarea} />

### Normal

This is how the form input component looks without any given props or data.

<Preview>
  <Story height="200px" name="Normal">
    {{
      components: { SysFormTextarea },
      template: `
        <sys-form-textarea
          id="normal"
          label="Input"
        />
      `
    }}
  </Story>
</Preview>

### With Placeholder

This is how the input component looks when you give it some placeholder text.

<Preview>
  <Story height="200px" name="Placeholder">
    {{
      components: { SysFormTextarea },
      template: `
        <sys-form-textarea
          id="placeholder"
          label="Input"
          placeholder="Placeholder Text"
        />
      `
    }}
  </Story>
</Preview>

### Disabled

This is how the input will look when it's disabled.

<Preview>
  <Story height="200px" name="Disabled">
    {{
      components: { SysFormTextarea },
      template: `
        <sys-form-textarea
          disabled
          id="disabled"
          label="Input"
          value="text"
        />
      `
    }}
  </Story>
</Preview>

### Invalid

This is how the input will look when it is invalid.

<Preview>
  <Story height="200px" name="Invalid">
    {{
      components: { SysFormTextarea },
      template: `
        <sys-form-textarea
          id="invalid"
          immediate
          label="Input"
          validation="required"
        />
      `
    }}
  </Story>
</Preview>
